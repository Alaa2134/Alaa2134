# Background videos

Drop vertical (9:16) nature/sky video loops here as `.mp4`, `.mov`, `.webm`,
or `.mkv`. The generator picks one at random per reel and scales/crops it to
1080x1920.

If this folder is empty, a calm gradient background is generated automatically,
so the platform works out of the box without any video files.

Only use footage you have the rights to publish.
